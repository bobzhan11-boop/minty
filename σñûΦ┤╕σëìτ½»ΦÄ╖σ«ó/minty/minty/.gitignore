node_modules
.next
out
build
*.log

# Secrets — never commit real env files (see .env.example for the keys)
.env
.env.local
.env*.local
.DS_Store

# Prisma SQLite dev database
prisma/dev.db
prisma/dev.db-journal
